"""evaluate the model"""

import torch
import utils
import matplotlib.pyplot as plt


if __name__ == "__main__":

    # Load the model checkpoint
    # chose a camera position
    # chose a camera direction
    # chose the size of the image

    # load the model

    # Perphorm inference

    # Render the image
    pass